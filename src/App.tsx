import { ReactElement } from "react";
import { Counter } from "./features/counter/Counter";

function App(): ReactElement {
  return (
    <div>
      <Counter />
    </div>
  );
}

export default App;
