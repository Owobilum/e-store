import type { SizeType } from '../../types'

export const DOLLAR_EURO_RATE = 1.08

export const DEFAULT_SIZE: SizeType = 'm'

export const TAX_RATE: number = 21 / 100

export const DEFAULT_ERROR_MESSAGE = 'sorry, there was an error'
